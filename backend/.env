# ====================================
# EVENT TRACKING SYSTEM - BACKEND CONFIGURATION
# ====================================

# ---------- SERVER ----------
PORT=5001
NODE_ENV=development

# ---------- MONGODB ATLAS ----------
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?options
# Replace with YOUR actual credentials from MongoDB Atlas

MONGODB_URI=mongodb+srv://saic24044:Samiksha0@cluster0.y9giw5i.mongodb.net/event_tracking?retryWrites=true&w=majority&appName=Cluster0
# ---------- REDIS (QUEUE) ----------
# Option 1: Local Redis (default)
REDIS_URL=redis://localhost:6379

# Option 2: Redis Cloud (uncomment and use if you don't have local Redis)
# REDIS_URL=redis://default:password@redis-12345.c90.us-east-1-3.ec2.cloud.redislabs.com:12345

# Option 3: Redis with password (local with auth)
# REDIS_URL=redis://:password@localhost:6379

# ---------- CORS ----------
# Frontend URL for CORS
CLIENT_URL=http://localhost:5173

# ---------- QUEUE SETTINGS ----------
# Maximum concurrent jobs per worker
QUEUE_CONCURRENCY=5

# Job retry attempts
JOB_RETRY_ATTEMPTS=3

# Job timeout in milliseconds (5 minutes)
JOB_TIMEOUT=300000

# ---------- LOGGING ----------
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Enable detailed job logging
ENABLE_JOB_LOGGING=true

# ---------- RATE LIMITING ----------
# Events per minute per IP
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# ---------- SECURITY ----------
# Session secret (generate your own for production)
SESSION_SECRET=your-secret-key-change-this-in-production

# API request timeout (30 seconds)
REQUEST_TIMEOUT=30000